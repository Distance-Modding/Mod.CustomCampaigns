/*using HarmonyLib;

namespace CustomCampaigns.Patches
{
    [HarmonyPatch(typeof(LevelGridMenu), nameof(LevelGridMenu.AddEntry))]
    internal static class LevelGridMenu__AddEntry
    {
        [HarmonyPrefix]
        internal static void EditAddCampaignButton(LevelGridMenu __instance, LevelGridMenu.PlaylistEntry entry)
        {
            if (entry.labelText_ == "Add Campaign")
            {
                
            }
        }
    }
}*/
